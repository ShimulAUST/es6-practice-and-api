//declare variable based on the name of an object

const myObject = { x: 2, y: 50, z: 600, a: 22, b: 255 };
const { x, y } = myObject;
console.log(x, y);

//destructuring array
const [p, q] = [45, 35, 55];
console.log(p, q);